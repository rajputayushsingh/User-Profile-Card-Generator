# User-Profile-Card-Generator
User Profile Card Generator is a full-stack web application built with Node.js, Express.js, HTML, CSS, and SQLite. It allows users to enter their name, bio, skills, GitHub, and LinkedIn links. The server processes the form data, generates a dynamic profile card, and stores profile records in a SQLite database.
